const styles = (theme) => ({
  root: {
    flex: 6,
    justifyContent: "center",
    alignItems: "center",
    textAlign: "center",
  },
  heading: {
    background: "#bde0ff",
    padding: 10,
  },
});

export default styles;
